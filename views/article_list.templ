package views

import (
	"fmt"
	"github.com/Ayomided/prog.git/sqlite"
)

templ ArticleList(articles []sqlite.Article) {
	@base() {
		<div class="max-w-2xl mx-auto px-4 py-8">
			<h1 class="text-2xl font-bold text-gray-800 mb-6">Blog Posts</h1>
			<ul class="space-y-4 list-disc list-inside">
				for _, article := range articles {
					<li>
						<a href={ templ.URL(fmt.Sprintf("articles/%v", article.Slug)) } class="text-blue-600 hover:underline">
							{ article.Title }
						</a>
						<span class="text-sm text-gray-500 ml-2">
							{ article.UpdatedAt.String() }
						</span>
					</li>
				}
			</ul>
		</div>
	}
}
